const товары = [
    { название: 'Товар 1', цена: 50 },
    { название: 'Товар 2', цена: 120 },
    { название: 'Товар 3', цена: 10 },
  ];
  
  let сумма = 0;
  for (const товар of товары) {
    сумма += товар.цена;
  }
  
  console.log('Сумма товаров:', сумма);
  
  for (const товар of товары) {
    if (товар.цена > 100) {
      console.log(`${товар.название} -> :(`);
    } else if (товар.цена < 20) {
      console.log(`${товар.название} -> :)`);
    }
  }